import { HeroSection } from "@/modules/HeroSection";

const HomePage = () => {
    return (
        <div>
            <h2> this is a home page </h2>
            <HeroSection/>
        </div>
    );
};

export default HomePage;