import { HeroSection } from "@/modules/HeroSection";

const HomePage = () => {
    return (
        <div className="h-screen">
            
            <HeroSection/>
        </div>
    );
};

export default HomePage;